// Perimortem Engine
// Copyright © Matt Kaes
dialect : App;

import Graphics : Package = Perimortem::Graphics;
import Runtime : Package = Perimortem::Runtime;
import SplashScreen : Scene = "splash_screen.ttx";

public func main[] -> [] {
  // Create a window for the thread so we can render scene objects.
  const window : Graphics::Window = Graphics::Window -> create(
    .title = "Perimortem",
    .window_size = (.width = 750, .height = 750),
  );

  // Set the initial scene and run it until scene is set to Void.
  Runtime -> start(
    .initial_scene = SplashScreen(),
    .window = window,
  );
}
