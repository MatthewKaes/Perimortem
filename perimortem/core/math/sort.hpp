// Perimortem Engine
// Copyright © Matt Kaes

#pragma once

#include "perimortem/core/access/structured.hpp"

#include <cmath>

namespace Perimortem::Core::Math {

template <typename type>
void sort(Access::Structured<type> data)
{
  std::log
    int depthLimit = 2 * log(end-begin);

    // Perform a recursive Introsort
    IntrosortUtil(arr, begin, end, depthLimit);

      return;
}

}