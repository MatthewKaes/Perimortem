// Library source acceptance.
dialect : Library;

@catalog("scalar")
@catalog("scalar")
public CountAlias : alias = Unsigned_64;

@presentation
public Mode : enum[Unsigned_8] {
  idle = 0;
  ready = 1;
}

@value_type
public Packet : struct {
  @nested_type
  public Nested : struct {
    @default_value
    public state value : CountAlias = 0;
  }

  @member
  public state nested : Nested;
}

@reference_type
public Session : object {
  @identity
  public state id : CountAlias;

  @state
  expose state ready : Bool = false;
}

@initializer
@abi("C")
public session : Session = new(.id = 7);

@abi("C")
@symbol("source_acceptance")
@tooling("entry")
@tooling("entry")
public exported : func = [] -> CountAlias {
  return 7;
}
